local RS=game:GetService('ReplicatedStorage');local C=require(RS.Modules.Shared['Config.Module']);local Net=require(RS.Net.Events);local ev=Net.Get('ChaosBroadcast');local CC={};function CC.Telegraph(n,d)print('[ChaosClient] Incoming:',n,'in',C.CHAOS.TELEGRAPH_SEC,'s (dur '..d..'s)')end;ev.OnClientEvent:Connect(function(p)if typeof(p)=='table' and p.name then CC.Telegraph(p.name,p.duration)end end);return CC
